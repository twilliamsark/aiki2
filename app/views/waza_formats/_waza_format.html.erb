<% if VideoUtils.visible_videos(waza_format.videos, current_user).any? %>
  <%= render 'wazas/waza_format_videos', waza_format: waza_format, format: format, video_id: video.id unless video.nil? %>
<% end %>
