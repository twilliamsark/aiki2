<% if @applied_technique
  @video = @applied_technique.first_video unless @video
%>
  $("#vid").replaceWith("<%= escape_javascript(render('show', applied_technique: @applied_technique, video: @video)) %>")
<% else %>
  $("#vid").replaceWith("<%= escape_javascript(render('shared/no_video')) %>")
<% end %>
