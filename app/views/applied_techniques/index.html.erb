<div class="row">
  <%= render 'shared/lnav', type: @type, sort_by: @default_sort, selection: @selection %>
  <% if @applied_technique
    at = exhibit(@applied_technique, self)
  %>
    <%= at.render_body(@video) %>
  <% else %>
    <%= render 'shared/no_video' %>
  <% end %>
</div>
